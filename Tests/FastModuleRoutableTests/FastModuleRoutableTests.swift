import XCTest
@testable import FastModuleRoutable

final class FastModuleRoutableTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FastModuleRoutable().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
